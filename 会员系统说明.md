# 拼豆DIY会员系统 - 安装与使用说明

## 系统概述

本系统为拼豆DIY店主计时收银后台系统新增会员模块，实现会员管理、充值、余额抵扣等功能。

## 功能特性

### 会员管理
- 新开会员卡（姓名、手机号、备注）
- 会员检索（手机号实时查询）
- 会员列表展示（序号、姓名、手机号、余额、开卡时间、累计充值、累计消费）
- 编辑会员信息（姓名、备注，手机号不可修改）
- 注销会员（软删除，数据保留）

### 会员充值
- 输入手机号自动带出会员信息和当前余额
- 输入充值金额确认充值
- 充值明细表格（充值时间、手机号、会员姓名、充值金额、充值后余额）
- 支持起止日期筛选充值记录

### 桌台结算会员抵扣
- 结算时可绑定会员卡（输入手机号）
- 显示会员当前余额
- 余额抵扣结账选项
- 余额不足提示

### 数据统计
- 当日会员充值总额
- 当日会员余额消费额
- 筛选期间会员充值、消费汇总
- 历史订单表格新增会员手机号列、支付方式列

### 订单历史
- 新增会员手机号筛选功能
- 显示会员手机号、支付方式

## 技术架构

- 前端：Vue3 + Vite + Tailwind CSS
- 后端：Node.js + Express + mysql2
- 数据库：MySQL

## 安装步骤

### 1. 安装MySQL并创建数据库

1. 确保MySQL服务已启动（localhost，账号root，密码123456）
2. 执行数据库初始化脚本：
   ```bash
   mysql -u root -p123456 < database/init.sql
   ```
   
   或在MySQL客户端中执行：
   ```sql
   source database/init.sql;
   ```

### 2. 安装依赖

#### 后端依赖
```bash
cd server
npm install
```

#### 前端依赖
```bash
npm install
```

### 3. 启动服务

#### 方式一：使用启动脚本（Windows）
双击运行 `start.bat`

#### 方式二：手动启动

**启动后端API服务：**
```bash
cd server
npm start
```
后端服务运行在 http://localhost:3000

**启动前端开发服务器：**
```bash
npm run dev
```
前端页面运行在 http://localhost:5173

## 使用说明

### 会员管理页面

1. 点击导航栏【会员管理】进入会员管理页面
2. **新开会员卡**：填写姓名、手机号（11位）、备注，点击【办理开卡】
3. **会员检索**：输入手机号，点击【查询】查看会员信息
4. **会员充值**：输入手机号自动显示会员余额，输入充值金额，点击【确认充值】
5. **会员列表**：查看所有会员，可编辑姓名/备注，可注销会员（余额为0才能注销）
6. **充值明细**：查看所有充值记录，支持日期筛选

### 桌台结算会员抵扣

1. 点击桌台进入操作弹窗
2. 在【绑定会员卡】输入框输入会员手机号
3. 自动显示会员姓名和当前余额
4. 勾选【余额抵扣】选项
5. 点击【结算账单】
   - 余额充足：自动扣除余额，订单标记为"会员余额"支付
   - 余额不足：弹窗提示，可选择取消会员抵扣或前去充值

### 数据统计会员相关

1. 点击【数据统计】查看会员相关统计
2. 顶部显示：当日会员充值、当日会员消费、筛选期间充值、筛选期间消费
3. 历史订单表格新增：会员手机号列、支付方式列

### 订单历史会员筛选

1. 点击【订单历史】查看所有订单
2. 输入会员手机号筛选该会员的所有消费记录
3. 表格显示会员手机号和支付方式

## 数据库表结构

### member 会员表
- id: 会员ID
- name: 会员姓名
- phone: 手机号（唯一索引）
- balance: 账户余额
- create_time: 开卡时间
- total_recharge: 累计充值总额
- total_consume: 累计消费总额
- remark: 备注
- status: 状态（1-正常，0-注销）

### member_recharge 充值记录表
- id: 充值记录ID
- member_id: 会员ID
- phone: 手机号
- recharge_money: 充值金额
- after_balance: 充值后余额
- create_time: 充值时间

### orders 订单表（新增字段）
- member_phone: 会员手机号
- pay_type: 支付方式（现金/会员余额）
- member_pay_amount: 会员余额支付金额

## 注意事项

1. **原有功能完全保留**：桌台计时、计价逻辑、套餐配置、三色状态等原有功能100%不变
2. **仅店主后台操作**：无顾客前端、无自助开卡、无线上支付
3. **数据持久化**：会员、充值、会员订单数据永久存MySQL，重启不丢失
4. **手机号唯一**：会员手机号不可重复，数据库设置唯一索引
5. **余额为0才能注销**：会员余额大于0时无法注销，需先处理余额

## API接口说明

后端API服务端口：3000

### 会员相关
- POST /api/member/create - 创建会员
- GET /api/member/search - 查询会员
- GET /api/member/list - 会员列表
- PUT /api/member/update - 更新会员
- PUT /api/member/cancel - 注销会员
- POST /api/member/recharge - 会员充值
- GET /api/member/recharge-history - 充值记录
- POST /api/member/consume - 会员消费

### 订单相关
- POST /api/order/create - 创建订单
- GET /api/order/list - 订单列表
- GET /api/order/statistics - 统计数据

## 生产环境部署

系统支持部署到线上环境，可以生成可访问的网址。

### 方案一：Railway部署（推荐，最简单）

[Railway](https://railway.app) 提供免费的Node.js应用托管，支持MySQL数据库。

#### 部署步骤：

**1. 准备代码**
- 将代码上传到GitHub仓库
- server文件夹为后端代码
- 根目录为前端代码

**2. 部署后端**
- 访问 https://railway.app
- 使用GitHub账号登录
- 点击 New Project → Deploy from GitHub
- 选择后端代码仓库
- Railway自动识别Node.js并部署

**3. 添加MySQL数据库**
- 在Railway项目中点击 Add Plugin → MySQL
- 数据库连接信息会自动生成

**4. 配置环境变量**
在Railway项目设置中添加：
```env
DB_HOST=your-mysql-host
DB_USER=your-mysql-user
DB_PASSWORD=your-mysql-password
DB_NAME=bead_timer
PORT=3000
```

**5. 获取后端URL**
部署成功后，Railway提供类似 `https://bead-timer-server.up.railway.app` 的URL

**6. 部署前端**
- 使用 Vercel (https://vercel.com) 或 Netlify (https://netlify.com)
- 导入前端代码仓库
- 设置构建命令：`npm run build`
- 设置输出目录：`dist`
- 添加环境变量：`VITE_API_BASE=https://your-backend-url.com/api`

---

### 方案二：阿里云/腾讯云服务器

适合想要完全控制的用户：

**1. 购买云服务器**
- 学生版约10元/月
- 选择Ubuntu 20.04或CentOS 7系统

**2. 安装宝塔面板**
```bash
# Ubuntu系统
wget -O install.sh https://download.bt.cn/install/install-ubuntu_6.0.sh && sudo bash install.sh

# CentOS系统
yum install -y wget && wget -O install.sh https://download.bt.cn/install/install_6.0.sh && sh install.sh
```

**3. 宝塔面板配置**
- 安装Nginx、MySQL、Node.js管理器
- 创建网站，绑定域名
- 上传前端代码到 `/www/wwwroot/你的域名/`
- 使用Node管理器部署后端

**4. 配置Nginx反向代理**
在网站设置中添加：
```nginx
location /api {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}

location / {
    try_files $uri $uri/ /index.html;
}
```

---

### 方案三：使用免费PAAS平台

**Render.com**
- 免费计划支持Node.js应用
- 支持MySQL数据库（免费250MB）

**Heroku**
- 免费Dyno（需信用卡验证）
- 支持PostgreSQL

---

## 环境变量配置

创建 `.env` 文件配置环境变量：

```env
# 后端数据库配置
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=123456
DB_NAME=bead_timer
PORT=3000

# 前端API地址（部署后修改）
VITE_API_BASE=http://localhost:3000/api
```

---

## 故障排查

### 后端服务无法启动
1. 检查MySQL是否启动
2. 检查数据库连接配置（server/db.js）
3. 检查数据库是否已创建（执行init-db.js）

### 前端无法访问后端API
1. 确认后端服务已启动（http://localhost:3000）
2. 检查API地址配置（src/utils/api.js）
3. 部署时确认环境变量 `VITE_API_BASE` 设置正确

### 会员数据无法保存
1. 检查MySQL连接
2. 检查数据库表是否创建
3. 查看后端日志错误信息

## 开发团队

拼豆DIY会员系统 - 2026