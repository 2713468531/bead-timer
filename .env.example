# 生产环境配置示例
# 将此文件复制为 .env 并填入实际值

# 后端API地址（部署后修改为实际地址）
VITE_API_BASE=https://your-backend-domain.com/api

# 数据库配置（在Railway等平台上会自动设置）
DB_HOST=your-mysql-host
DB_USER=your-mysql-user
DB_PASSWORD=your-mysql-password
DB_NAME=bead_timer
